const express = require("express");
require("dotenv").config();

// init server
const app = express();
//test

// routes ?

// middlewares

// middlewares
app.listen(process.env.PORT, () => {
  // Insert Poller
  // Poll the four things
});
